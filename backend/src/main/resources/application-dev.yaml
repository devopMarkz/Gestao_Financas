spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/controle_financas
    username: postgres
    password: aluno
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  flyway:
    url: jdbc:postgresql://localhost:5432/controle_financas
    user: postgres
    password: aluno
    schemas: public
